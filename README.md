# Subscription Management System (UI)

Static multi-page UI using **Bootstrap 5 only** (no animations, gradients, glassmorphism, or dark theme).

Open `index.html` in a browser.

> Note: All “CRUD” actions are demo-only and update in-memory data using `localStorage`.

